import React from 'react';

const Home = () => {
    return (
        <div>
            <div>Im the Home Component designed by kalanithi</div>
            <button onClick={() => console.log('button clicked')}>Click Me</button>
        </div>
    );
};

export default Home;